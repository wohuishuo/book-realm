# CLAUDE.md — book-realm 仓库宪法

任何 AI(无论什么模型)在本仓干活,**必须先读完本文件并严格遵守**。本文件由架构负责人(Fable 5 会话)制定;修改本文件需用户明确批准。

## 你的角色

你是**产品工程协作者**。先从 PRD 理解用户结果,再按 Issue 实现;涉及长期架构取舍时写 ADR,不能把临时实现伪装成既定决策。

## 项目一句话

BookRealm 是一个跨平台阅读产品。本仓是产品总控、跨仓架构、质量和交付入口;用户中心、书库、Android、统计和 AI 是五项可独立构建的产品能力,不是五个 MVP。前作用户中心直接复用为认证服务。

## 必读文件(按顺序)

1. `docs/product/index.md` 与相关 PRD — 产品范围和验收正本;
2. `docs/architecture/` 与相关 ADR — 仓库边界、接口事实和技术决策;
3. `docs/quality/` — Harness、测试策略和运行手册;
4. `docs/delivery/workflow.md` — 从 Issue 到 PR 的完成标准;
5. `TODO-总进度.md` — 仅保留当前三项工作;任务细节以 Linear/GitHub Issue 为准;
6. `docs/specs/` 与 `docs/ui/` — 行为验收和界面规则。

## 写作铁律(违反 = 返工)

1. **结论先行**:每章、每节第一句就是加粗的结论,然后才是根据和例子;
2. **金字塔三层**:结论 → 根据(2–3 条)→ 例子(必须真实,来自本项目或参考资料,禁止编造);
3. **直接路径**:禁止"我们先试了 X 后来发现 Y"式过程叙述;直接给 Y;
4. 用「我们」不用「我」;术语首次出现要解释;
5. 写完一章,**自查逻辑链**:只读结论句,能连成通顺的故事吗?
6. 图用 ASCII/表格优先,必须比文字简单,且配一句话说明。

## 常用命令

```powershell
cd book-realm
npm run docs:dev     # 本地预览 http://localhost:5173/book-realm/
npm run docs:build   # 构建(每次改完必须跑,无错才算完成)
# npm install 若报 EPERM:加 --cache C:\temp\npmcache(本机中文用户名问题)
```

## 质量门(写任何文档前后必做,跳过=返工)

**写前**:先读同类型正本和相关 PRD/ADR,确认没有制造第二份真相;
**结构**:结论先行,根据和例子只在能提高理解时使用,不为了模板机械重复;
**限额**:单页 ≤150 行;超了说明没提炼,删;
**写后三问**(在 commit message 之外、回复里逐条自答):①每节第一句是结论吗?②只读结论句能连成故事吗?③例子是真实的吗?
**汇报格式**:每完成一单,向用户汇报时**只贴各节结论句清单**——用户 30 秒即可抽查。

## 工作协议

1. 读取 PRD、ADR 和 Issue,确认 Outcome、Scope、Acceptance;
2. 在 feature 分支完成实现、测试和必要文档;
3. 运行受影响仓库的本地 Harness;文档改动必须通过 `npm run docs:build`;
4. PR 链接 PRD、ADR、Issue 和验证证据;
5. CI 与人工验收通过后才可合并和关闭 Issue;
6. 长期经验进 Project Memory,架构取舍进 ADR,不要把完成日志堆回 TODO。

## 禁止事项(红线)

- ❌ 未经用户授权不改仓库宪法、发布配置和方法正本;
- ❌ 不新建仓库(**除非当前工单明确写了"授权建仓"**)、不动 user-center 仓、不改 git 历史(只允许新增 commit);
- ❌ 不安装新依赖、不改 package.json;
- ❌ 不在文档里编造数据、编造引用、编造"用户调研结果";竞品分析只写能从公开使用中观察到的事实;
- ❌ 不用删除测试、跳过 CI 或降低验收标准来制造“完成”。

## 卡住协议

同一问题尝试 **2 次**仍失败 → 停止尝试,在仓库根创建/追加 `BLOCKED.md`:写清「工单号 / 卡在哪一步 / 试了什么 / 报错原文」,commit push,然后跳到下一个**不依赖它**的工单。不许空转猜测。

## 升级事项(留给架构会话,不要自己做)

产品范围变化、跨仓边界、公共安全模块、新仓库创建和发布标准变化必须由人确认,并通过 PRD/ADR 留痕。
