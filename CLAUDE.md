# CLAUDE.md — book-realm 仓库宪法

任何 AI(无论什么模型)在本仓干活,**必须先读完本文件并严格遵守**。本文件由架构负责人(Fable 5 会话)制定;修改本文件需用户明确批准。

## 你的角色

你是**执行工程师**。架构与方法已定,你的工作是**按工单执行**,不是重新设计。
遇到工单没覆盖的决策 → 不要自作主张,按下方「卡住协议」处理。

## 项目一句话

超级项目「书域」= 5 个互相咬合的完整 MVP(电子书平台),星系结构:本仓是枢纽(平台书 + P 阶段文档 + MVP 地图),每个 MVP 另有独立仓。前作用户中心已完成(<https://github.com/wohuishuo/user-center-team-project>),直接复用为认证服务。

## 必读文件(按顺序)

1. `TODO-总进度.md` — 进度与下一步(唯一权威存档点;**当前该执行哪份工单以它的"工单队列"为准**)
2. 当前工单文件(`工单-*.md`,由 TODO 或用户指定;P 阶段已完成,勿重做)
3. `docs/guide/writing-style.md` — 写作铁律(金字塔)
4. `docs/platform/plan.md` — 完整蓝图(背景知识)

## 写作铁律(违反 = 返工)

1. **结论先行**:每章、每节第一句就是加粗的结论,然后才是根据和例子;
2. **金字塔三层**:结论 → 根据(2–3 条)→ 例子(必须真实,来自本项目或参考资料,禁止编造);
3. **直接路径**:禁止"我们先试了 X 后来发现 Y"式过程叙述;直接给 Y;
4. 用「我们」不用「我」;术语首次出现要解释;
5. 写完一章,**自查逻辑链**:只读结论句,能连成通顺的故事吗?
6. 图用 ASCII/表格优先,必须比文字简单,且配一句话说明。

## 常用命令

```powershell
cd book-realm
npm run docs:dev     # 本地预览 http://localhost:5173/book-realm/
npm run docs:build   # 构建(每次改完必须跑,无错才算完成)
# npm install 若报 EPERM:加 --cache C:\temp\npmcache(本机中文用户名问题)
```

## 质量门(写任何文档前后必做,跳过=返工)

**写前**:先完整读一个范例(理论类读 user-center 书 `theory/02-requirements.md`,技术类读 `stack/spring-boot.md`,本仓 `docs/platform/p5-domain.md` 也是合格范例),**模仿其结构再下笔**;
**模板锁定**:每节固定三段——`**结论:…**` / `**根据**:…` / `**例子**:…`,不许写成无结构长文;
**限额**:单页 ≤150 行;超了说明没提炼,删;
**写后三问**(在 commit message 之外、回复里逐条自答):①每节第一句是结论吗?②只读结论句能连成故事吗?③例子是真实的吗?
**汇报格式**:每完成一单,向用户汇报时**只贴各节结论句清单**——用户 30 秒即可抽查。

## 工作协议(每个工单的固定流程)

1. 读工单 → 读它指定的输入文件;
2. 写/改指定的**那一个**输出文件(不碰其他文件);
3. `npm run docs:build` 必须通过;
4. 按工单里的「验收清单」逐项自查,全过才算完成;
5. commit(格式:`P1 业务定位:一句话描述`)并 push;
6. 更新 `TODO-总进度.md` 对应勾选,一并 commit push。

## 禁止事项(红线)

- ❌ 不改 `docs/.vitepress/` 下任何文件(config/主题/配色已定);
- ❌ 不改方法正本:`writing-style.md`、`methodology.md`、`rc-checklist.md`、本文件;
- ❌ 不新建仓库(**除非当前工单明确写了"授权建仓"**)、不动 user-center 仓、不改 git 历史(只允许新增 commit);
- ❌ 不安装新依赖、不改 package.json;
- ❌ 不在文档里编造数据、编造引用、编造"用户调研结果";竞品分析只写能从公开使用中观察到的事实;
- ❌ 一个工单未完成验收,不开始下一个。

## 卡住协议

同一问题尝试 **2 次**仍失败 → 停止尝试,在仓库根创建/追加 `BLOCKED.md`:写清「工单号 / 卡在哪一步 / 试了什么 / 报错原文」,commit push,然后跳到下一个**不依赖它**的工单。不许空转猜测。

## 升级事项(留给架构会话,不要自己做)

P5 的 BC 边界终审、P7 的架构决策变更、任何 MVP 仓的创建、RC 终审。这些工单会标注【架构会话】。
