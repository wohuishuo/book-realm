# 工单 · P 阶段(执行工程师专用)

> 逐个执行,**不许跳序**(P5 依赖 P3/P4,P7 依赖 P5)。每个工单走完 `CLAUDE.md` 的工作协议六步才算完成。所有输出写进 `docs/platform/` 对应文件(占位结构已建好,把 TODO 换成正文,删掉"待完成"提示框)。

---

## 工单 P1:业务定位 → `p1-positioning.md`

**输入**:`docs/platform/plan.md` 第〇、一节;用户中心书的序言(风格参考)。
**写什么**:
- 业务领域:数字阅读 / 移动互联内容平台;
- 开发目标:三类用户(游客/读者/管理员)各自得到什么;平台解决"内容分散、阅读体验割裂、无智能辅助"什么具体问题;
- 用户画像:2–3 个具体画像(如"通勤碎片阅读的学生"),每个画像 = 场景 + 痛点 + 我们的回应。
**验收**:结论先行;每个画像有场景有痛点;无编造数据;≤120 行。

## 工单 P2:竞品分析 → `p2-competitors.md`

**输入**:起点读书 App、微信读书 App、番茄小说 App 的**公开可观察功能**(应用商店介绍、实际使用界面)。
**写什么**:
- 对比表:维度=书架/书城/阅读器/书评/AI 功能/商业模式,三列竞品 + 一列「书域(我们)」;
- 每个竞品一段:它最强的一点 + 我们不与之竞争的理由;
- 我们的差异点:**教学完整性**(全流程开源+书)+ RAG 读书问答;
**红线**:只写实际观察到的功能,禁止编造市场份额/用户数等数据。
**验收**:对比表完整;差异点说得清"为什么用户选我们的场景存在";≤150 行。

## 工单 P3:功能分解树 → `p3-features.md`

**输入**:`plan.md` 第一节 MVP 地图、第三节里程碑。
**写什么**:ASCII 功能树,根=书域平台,第一层=5 个 MVP,第二层=每 MVP 的功能(对照里程碑 DoD),叶子≤3 层;每个 MVP 树下一句话说明边界(做什么/不做什么);标注砍单顺序。
**验收**:树与 plan.md 的 MVP/DoD 一致,无新增功能;每个叶子在后续里程碑里能找到归属。

## 工单 P4:用例分析 → `p4-usecases.md`

**输入**:P1 的用户画像、P3 的功能树。
**写什么**:
- 用户类型与权限表(游客/读者/管理员 × 能做什么);
- 用例图(ASCII 即可,参考用户中心书 theory/02 的画法);
- **3 个核心用例规约**(表格式:用例名/主角色/前置条件/主成功流程/扩展流程/后置条件):①注册登录(可大量参考用户中心书)②打开一本书并阅读 ③AI 读书问答。
**验收**:三个规约的每一步都能对应到某个 MVP 的某个接口/界面;格式与用户中心书 02 章一致。

## 工单 P5:领域模型与 BC 映射 → `p5-domain.md` 【架构会话终审】

**输入**:P3、P4 产出;旧 Android 项目实体(`android 项目/MyApplication/.../model/` 的 13 个实体文件,精简思路:首期只保留 Book/Chapter/Paragraph/Tag);参考资料:云图库 2025 的 ddd*.png 架构图。
**写什么**:
- 五个 BC(用户/书库/阅读/统计/AI)各自的:实体清单、Service 职责、对外接口或事件;
- 领域事件表:UserLogin、ReadingProgress(载荷字段、生产者、消费者);
- ASCII 类图(每 BC 内主要实体)+ BC 映射图(BC 之间传什么);
**注意**:初稿即可,边界拿不准的地方**明确标注「待架构终审」**,不要硬定。
**验收**:每个 BC 的实体不与其他 BC 重叠;事件表字段齐全;待定项已标注。

## 工单 P6:动态建模 → `p6-dynamics.md`

**输入**:P4 的三个用例、P5 的事件表。
**写什么**:三张 ASCII 顺序图——①登录事件流(App→用户中心→RabbitMQ→日志/统计)②打开一本书(App→书库→Room 缓存)③AI 问答(App→AI 服务→向量库→LLM);每张图配一段金字塔说明。
**验收**:图中每个参与者都是 P5 里存在的 BC/服务;箭头上写明传的是什么。

## 工单 P7:架构与技术选型 → `p7-architecture.md` 【架构会话终审】

**输入**:P5、P6;`plan.md` 第二节技术栈表;用户中心书的 tech-landscape 章(写法范本)。
**写什么**:架构总图(plan.md 一节已有雏形,细化:加端口、数据库归属、消息队列 exchange/队列名);技术选型表(每项一句"为什么是它而不是替代品");部署图(docker compose 内有哪些容器)。
**验收**:图与 P5 BC 一一对应;选型表覆盖前后端+中间件;写法对齐 tech-landscape 章。

## 工单 P8:分工与计划 → `p8-schedule.md`

**输入**:`plan.md` 第三节(五周里程碑);结课设计说明的流程管理要求。
**写什么**:五周时间轴表(周×MVP×DoD);风险清单(≥4 条,每条带对策);砍单顺序及触发条件("到第 X 周 Y 未完成则砍 Z");分工表留空位(组员信息由用户填)。
**验收**:时间轴与 plan.md 一致;每条风险有具体对策不喊口号。

---

## 全部完成后(P 阶段 RC)【架构会话】

按 `docs/guide/rc-checklist.md` 第一、二层自查并修订,然后在 `BLOCKED.md`(若有)和 TODO 里汇总状态,**停在这里等架构会话做 RC 终审**——不要自行开建 MVP 仓。
