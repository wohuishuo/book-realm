# Global UI Rules

- 每个页面最多一个主按钮。
- 主按钮只用于当前页面最重要的操作。
- 加载、空、错误、成功和无权限状态必须明确。
- 同一行为使用同一图标、名称和交互反馈。
- 页面标题、返回行为和导航选中状态必须一致。
- 不用营销式大卡片装饰操作型页面。
- 不嵌套卡片,不使用无功能的装饰渐变或光斑。
- 文本、按钮和工具栏在小屏幕上不得重叠或截断。
- 所有可点击元素必须有稳定尺寸和可访问标签。

